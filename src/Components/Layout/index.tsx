import React from "react";
import "./Layout.scss";
import Logo from "../Logo";
const Layout = () => {

    return (
        <nav className="Nav">
            <div>
                <Logo/>
            </div>
        </nav>
    )
}

export default Layout;